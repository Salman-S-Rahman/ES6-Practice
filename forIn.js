
var studentInfo = {
    name: "Taskin Asghar",
    id: 44,
    studies: "EEE",
    pashion: "MERN"
}

for (let info in studentInfo) {
    console.log(`${info} : ${studentInfo[info]}`);
}

// ============= for of simply use for in array ==========