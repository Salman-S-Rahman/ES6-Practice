function totalNumber(number1, number2) {
    let total = 0;
    console.log(arguments);
    for (number of arguments) {
        total = total + number
    }
    return total;
}
const sum = totalNumber(10, 20, 30, 40, 50);

console.log("Total = " + sum);